using UnityEngine;
using System.Collections;

public class World_top_pos : MonoBehaviour {
    
    public Vector3 pos_bot;
	// Use this for initialization
	void Start () {
        pos_bot = transform.position;
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
